class AppConfig {
  // Replace this base URL with your real backend API endpoint.
  static const todoApiBaseUrl = 'https://api.example.com';

  // Enable remote sync for TodoRepository when an API base URL is configured.
  static const enableRemoteSync = false;
}
